{
  // 指定 ArcSAR 產生的參數檔路徑，程式需要讀取這個檔案來確認掃描的基本設定
  "parameter_file": "C:/ArcSAR_Data/20251030.image.txt",

  // 監測點列表 (Indices)：這是我們之前選定的那些 PS 點的 Index
  // 程式只會計算這幾個點，不會計算整張圖 30 萬個點，以確保速度
  "monitor_indices": [4363, 4483, 5036, 6341, 10025],

  // 警示門檻值 (單位：mm)：
  // 依據新的策略，如果「彈性回彈量」超過這個數值，就會發送警報
  "threshold_mm": 5.0,

  // 同調性門檻 (Coherence Threshold)：
  // 如果某個點的同調性低於這個值 (代表品質太差或雜訊)，程式會忽略該點，不進行計算
  "coherence_threshold": 0.4,

  // 起始頻率 (Start Frequency, Hz)：
  // 這是計算相位轉位移公式中 (c / 4*pi*f) 需要用到的參數，請參考 image.txt 內的設定
  "fc_start": 1.7e10, 

  // 頻寬 (Bandwidth, Hz)：
  // 用來計算中心頻率用的，同樣請參考 image.txt
  "bw": 4.0e8
}